/* 

# Uppercase constants

There is a widespread practice to use constants as aliases
for difficult-to-remember values that are known prior to execution.

Constants that are known prior to execution
(like a hexadecimal value for red)

In another word , capital-named constants are only used as aliases
for “hard-coded” values. 

*/

const COLOR_RED = '#F00';
const COLOR_GREEN = '#0F0';

console.log({ COLOR_GREEN, COLOR_RED });

require('../../projects/assets/log-counter');

const hello = 'Hello';
const world = 'World';
const sentence = hello.concat(' ', world);
// const sentence = text1.concat(...[' ', world]);

console.log(sentence);

/* All string methods return a new string. They don't modify
the original string. */
